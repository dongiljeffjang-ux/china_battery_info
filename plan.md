# China Battery Lens 향후 작업 계획

## 현재 상태

- 중국어·영어 RSS/뉴스 API 후보 수집기와 정규 기업 ID 매핑을 구현했다.
- 후보 376건을 SQLite 정독 검수 큐에 적재했다.
- 과거(90일 초과)는 공시·IR 우선, 최근 90일은 회사 발표 또는 복수 주요 언론 확인을 허용하는 출처 정책을 적용했다.
- 정적 MVP에는 Daily 1페이지 요약, 전체 뉴스 기반 Top 10, 헤드라인 Sankey, 회사별 뉴스, 기업 레이어×시간 매트릭스, Excel 내보내기, 중앙 시간축 기업 비교를 구현했다.
- 현재 화면의 이벤트는 시드 데이터다. 실제 후보를 정독·번역·승인한 데이터와 아직 연결되지 않았다.

## 1. 정독·번역·사실 추출 worker

### 목표

검수 큐의 승인 대상 후보를 읽고, 한국어 팩트와 인용 가능한 근거를 Event 후보로 만든다.

### 작업

- 소스별 본문 추출 adapter를 만든다. 공식 공시/PDF는 파일·페이지·문장 위치를 기록하고, 뉴스는 본문을 처리 중에만 사용한다.
- 번역 모듈을 연결한다. 원문 언어·원문 제목·한국어 제목·한국어 요약을 분리 저장한다.
- 회사, 사건일, 사건 유형, 수치, 지역, 내수/해외, 출처 상태를 구조화한다.
- 기사 중복·전재를 Canonical Event 후보로 묶는다.
- 원문 본문은 뉴스 DB에 장기 보관하지 않고, 한국어 요약·출처 URL·근거 위치·해시를 남긴다.

### 완료 기준

- 승인된 기사 1건이 한국어 요약과 출처 근거를 포함한 Event 후보로 저장된다.
- 공식 공시와 뉴스의 근거 상태가 구분된다.
- 같은 사건의 재보도가 중복 Event를 만들지 않는다.

## 2. 중요도와 시계열 핵심성 판정

### 목표

Daily Top 10 선정과 기업 시계열 표시를 서로 독립적으로 판단한다.

### 작업

- 기존 라벨링 엑셀의 `Top 10 / 회사별 중요 / 제외` 결과를 입력한다.
- Daily 중요도: 영향 규모·확정성·신규성·출처 신뢰도·산업 파급을 사용한다.
- 시계열 핵심성: 상태 변화·구체성·지속성·회사 직접성·중복 여부를 사용한다.
- 시계열 결과를 `핵심 / 참고 / 제외`로 분류한다.
- 단일 제3자 언론은 핵심 시계열·Top 10에서 제외한다. 회사 직접 발표, 복수 주요 언론, 또는 명시적 1차 출처 인용은 예외로 검수한다.

### 완료 기준

- 같은 뉴스가 Top 10 미선정이면서 시계열 핵심으로 저장될 수 있다.
- 후속 공시가 나오면 기존 언론 Event의 근거 상태가 공식으로 갱신된다.

## 3. 관리자 검수 화면

### 목표

자동 추출 결과를 사람이 승인·수정·반려하는 운영 화면을 만든다.

### 작업

- 검수 대기열: 원문/한국어 제목, 회사, 출처, 날짜, 근거 상태, 자동 분류를 표시한다.
- 검수 폼: 한국어 팩트, Top 10 여부, 시계열 핵심성, 시장·기술 레이어, 지역, 이벤트 날짜를 수정한다.
- 승인·반려·보류와 수정 이력을 기록한다.
- 중요도 라벨링 결과를 CSV/XLSX로 내보내고 다시 불러온다.

### 완료 기준

- 검수자가 Event 하나를 승인하면 재처리 없이 Daily와 기업 화면의 데이터 원본이 된다.
- 모든 승인 Event는 최소 하나 이상의 출처 URL과 근거 상태를 갖는다.

## 4. 실제 데이터 기반 Daily 연결

### 목표

시드 데이터를 제거하고, 승인 Event로 Daily를 매일 자동 생성한다.

### 작업

- LLM이 승인 Event만 사용해 1페이지 Daily 보고서를 생성한다.
- 전체 뉴스 후보에서 검증 통과 Top 10을 뽑고 원문 링크·한국어 팩트·선정 근거를 표시한다.
- 회사별 뉴스에는 승인된 이벤트를 회사 기준으로 누적한다.
- Sankey는 승인된 주요 헤드라인의 회사 → 사건 유형 → 산업 신호 태그로 생성한다.

### 완료 기준

- Daily 문장마다 근거 Event와 출처 URL이 연결된다.
- Top 10이 부족한 날은 임의로 10건을 채우지 않고 실제 검증 건수만 표시한다.

## 5. 기업 시계열 백필과 화면 연결

### 목표

각 회사의 최소 최근 3년 Event를 공식자료 중심으로 채운다.

### 작업

- 연차·반기·분기 보고서, 거래소 공시, IR에서 과거 Event를 백필한다.
- 최근 90일에는 주요 언론으로 공시 공백을 보완하고, 후속 공식자료로 갱신한다.
- 시장 4개 레이어와 기술 4개 레이어를 각 Event에 부여한다.
- 레이어×시간 매트릭스와 Excel 내보내기를 실제 DB 데이터로 교체한다.
- 비교 화면은 두 회사의 공통 시간축 위에 승인 Event만 배치한다.

### 완료 기준

- 초기 대상 회사별로 최소 최근 3년의 공식 근거 Event가 존재한다.
- 빈 셀은 “사건 없음”이 아니라 “확인된 이벤트 없음”으로 표시한다.

## 6. 배포·운영 기반

### 목표

Vercel 배포와 매일 자동 실행이 가능한 운영 환경을 만든다.

### 작업

- 환경변수: `FINLIGHT_API_KEY`, LLM API 키, 데이터베이스 연결값을 Vercel에 설정한다.
- 수집·정독·Daily 생성의 스케줄 실행과 실패 재시도를 구현한다.
- 실행 로그, 소스별 오류, 기사 처리 상태를 기록한다.
- DB/파일 저장소를 Vercel의 영구 저장소로 이전한다.

### 완료 기준

- 3일 연속 무인 수집·정독·Daily 생성에 성공한다.
- 한 소스 또는 API 오류가 전체 Daily 생성을 막지 않는다.

## 권장 실행 순서

1. 정독·번역·사실 추출 worker
2. 중요도·시계열 핵심성 판정
3. 관리자 검수 화면
4. 실제 데이터 기반 Daily 연결
5. 기업 시계열 3년 백필
6. 배포·스케줄·운영 안정화

